stages:
  - build
  - test
  - deploy

include:
  - remote: "{{ .DevGuardCiComponentBase }}/templates/full.yml"
    inputs:
      asset_name: "$DEVGUARD_ASSET_NAME"
      token: "$DEVGUARD_TOKEN"
      api_url: "$DEVGUARD_API_URL"
      web_ui: "{{ .DevGuardFrontendUrl }}"